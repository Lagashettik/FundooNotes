import { StyleSheet } from 'react-native';
import { globalStylesheet } from './global.styles';

export const RBSheetStyleSheet = StyleSheet.create({
    RBSheetButton : {
        ...globalStylesheet.button
    }
})